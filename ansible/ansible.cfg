[defaults]
inventory = inventory/hosts.ini
private_key_file = ~/.ssh/redis_cluster_key
host_key_checking = False
remote_user = ansible
roles_path = /home/dmin/redis-cluster-tool/ansible/roles
remote_tmp = /tmp/.ansible

[privilege_escalation]
become = True
become_method = sudo
become_user = root
